# Dev overrides — install with:
#   helm upgrade --install homelab platform/helm/homelab \
#     -f platform/helm/homelab/values.yaml \
#     -f platform/helm/homelab/values-dev.yaml

backend:
  environment: gitops-dev

ingress:
  host: homelab-dev.local
